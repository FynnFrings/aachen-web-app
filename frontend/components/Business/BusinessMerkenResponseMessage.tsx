import styles from "@/styles/business_response_message.module.scss";
const BusinessMerkenResponseMessage = () => {
  return (
    <div className={styles.container}>
      <h2 className={styles.text}>Please download the app!</h2>
      <div className={styles.fill}></div>
    </div>
  );
};

export default BusinessMerkenResponseMessage;
